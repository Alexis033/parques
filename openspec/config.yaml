# OpenSpec config — Parchis
# Updated by sdd-init (hybrid mode) — 2026-05-23

context: >
  Parchis (Colombian Parqués) board game built with Angular 21.2
  standalone components, CSS Grid board layout, pnpm monorepo with 3
  workspace libs (@parchis/engine, @parchis/shared, @parchis/supabase),
  TypeScript 5.9 strict mode, ES2022 target. Unit/integration tests use
  vitest (libs/), E2E uses Playwright. Supabase for backend/auth.

strict_tdd: true

testing:
  runner: vitest
  layers:
    unit: true
    integration: false
    e2e: true
  coverage: true
  linter: null
  type_checker: tsc (strict via ng build)
  formatter: null

phases:
  proposal: sdd-propose-1
  spec: sdd-spec-1
  design: sdd-design-1
  tasks: sdd-tasks-1
  apply: sdd-apply-1
  verify: sdd-verify-1
  archive: sdd-archive-1
